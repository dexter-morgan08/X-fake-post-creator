import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Mockpost Studio — Fictional Social Post Mockups",
  description:
    "Create fictional social-media post mockups for design, prototyping and educational use. Client-side only, with persistent FICTIONAL MOCKUP labeling.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className={`${geistSans.variable} ${geistMono.variable} h-full`}>
      <body className="min-h-full flex flex-col bg-[#f4f4f5] text-zinc-900">
        {children}
      </body>
    </html>
  );
}
